pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "android"   // الاسم مو مهم كثير، خله زي ما تحب

include ':app'
include ':capacitor-android'
include ':capacitor-cordova-android-plugins'
