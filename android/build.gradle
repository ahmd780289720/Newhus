plugins {
    id 'com.android.application' version '8.1.2' apply false
}

// ما نضيف أي repositories هنا عشان ما يتعارض مع settings.gradle

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
