plugins {
    id 'com.android.application' version '8.1.2' apply false
    id 'com.capacitorjs.capacitor-android-gradle-plugin' version '5.0.0' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
